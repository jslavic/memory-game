import React from "react";
import { TitleDiv } from "../../styles";

/**
 * Title provides the title that will be displayed on the header
 * TitleDiv provides font styling
 * Another styled div and a logo can be added in the future
 */

const Title = () => {
    return (
        <TitleDiv>Giphy memory</TitleDiv>
    )
}

export default Title
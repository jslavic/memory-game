import styled from "styled-components";

/**
 * Display score inside the header as the most-right item
 */

const ScoreboardContainer = styled.div`
    display: flex;
    align-items: center;
    gap: 10px;
`;

export default ScoreboardContainer